<style type="text/css">
  /* == Player watermark Please don't romeve css ==; */
  .vjs-quality-button.vjs-menu-button.vjs-menu-button-popup.vjs-control.vjs-button {
    opacity: 1 !important;
    display: block !important;
    overflow: visible !important;
  }

  .drm-audio-new-wrpaer .video-js .vjs-slider {
    top: 109px !important;
    margin: 0 !important;
    background: #fff !important;
  }

  .music-card {
    z-index: 0 !important;
  }

  .vjs-loading-spinner {
    display: none;
    visibility: visible;
    z-index: 99999999999;
    margin-top: -33%
  }

  .positionRelative {
    position: absolute !important;
    width: fit-content;
    right: 0;
    margin-top: -3px;
    cursor: pointer;
  }

  .qualitymenu {
    right: 0;
   top:inherit !important;
   bottom:41px !important;

  }

  .video-js.vjs-paused:not(.vjs-has-started) .vjs-loading-spinner,
  .video-js.vjs-paused:not(.vjs-has-started) .vjs-loading-spinner {
    z-index: 99999999999;
    -webkit-animation: vjs-spinner-spin 1.1s cubic-bezier(0.6, 0.2, 0, 0.8) infinite, vjs-spinner-fade 1.1s linear infinite;
    animation: vjs-spinner-spin 1.1s cubic-bezier(0.6, 0.2, 0, 0.8) infinite, vjs-spinner-fade 1.1s linear infinite;
  }

  button.vjs-quality-button.vjs-icon-hd.vjs-icon-placeholder.vjs-menu-button.vjs-menu-button-popup.vjs-button {
    opacity: 1 !important;
    display: block !important;
    overflow: visible !important;
  }

  .vjs-button>.vjs-icon-placeholder:before {
    font-size: 1.8em;
    line-height: 1.67;
    color: red;
    filter: invert(0);
  }

  .drm-audio-new-wrpaer .video-js .vjs-progress-control {
    width: 100% !important;
    left: 0 !important;
  }

  .drm-audio-new-wrpaer .video-js.vjs-progress-holder {
    height: 6pc !important;
  }

  .drm-audio-new-wrpaer .vjs-mouse-display {
    display: none !important
  }

  .drm-audio-new-wrpaer .vjs-time-tooltip {
    display: none !important
  }

  .drm-audio-new-wrpaer .vjs-quality-button.vjs-menu-button.vjs-menu-button-popup.vjs-control.vjs-button {
    margin-bottom: -54%;
    /* position: absolute; */
    margin-left: 63%;
    z-index: 9999999999999;
  }

  .drm-audio-new-wrpaer .vjs-menu-button-popup .vjs-menu {
    top: 0 !important;
    padding: 0;
  }

  .drm-audio-new-wrpaer .video-js {
    height: 0px !important;
  }

  .ad_setting_pb {
    opacity: 0;
  }

  .drm-audio-new-wrpaer .video-js .vjs-load-progress div {
    background: #fff !important;
  }

  .drm-audio-new-wrpaer .vjs-progress-control.vjs-control {
    margin: 0 !important;
  }

  .drm-audio-new-wrpaer.video-js .vjs-progress-holder {
    height: 6px !important;
  }

  .drm-audio-new-wrpaer .video-js .vjs-play-progress:before {
    font-size: 1.5rem !important;
    top: -8px !important;
  }

  .drm-audio-new-wrpaer button.vjs-fullscreen-control.vjs-control.vjs-button {
    display: none;
  }

  .vjs-icon-circle:before,
  .video-js .vjs-play-progress:before,
  .video-js .vjs-volume-level:before {
    color: var(--pbc) !important;
  }

  .drm-audio-new-wrpaer .vjs-current-time.vjs-time-control.vjs-control {
    display: none;
  }

  .drm-audio-new-wrpaer .vjs-duration.vjs-time-control.vjs-control {
    display: none;
  }

  span.vjs-icon-placeholder {
    display: none;
  }

  .vjs-time-control.vjs-time-divider {
    display: none;
  }

  .vjs-menu-button-popup .vjs-menu {
    top: 0 !important;
    padding: 0;
  }

  .vjs-menu-button-popup .vjs-menu {
    width: 0 !important;
  }

  .vjs-menu-button-popup .vjs-menu .vjs-menu-content {
    bottom: -3em !important;
    right: -39px !important;
    left: inherit !important;
    width: 16em !important
  }

  .section {
    overflow-y: auto !important;
  }

  /* ------------------------Episode Css Start 01-03-2024 -------------------- */

  .play_epsode_btn span img {
    width: 40px !important;
    height: 40px !important;
  }

  .share_btn_icon {
    font-size: 18px;
    background: #2b2b2b;
    padding: 0px 12px;
    border-radius: 5px;
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .nav-audio-list {
    align-items: center;
    font-size: 18px;
  }

  .pb_vd_card .pb_card_content {
    height: 25% !important;
  }

  .pb_episode_cont {
    position: relative;
    margin-top: -270 px;
    z-index: 999;
  }

  .bg_btn_color {
    color: var(--pbc) !important;
  }

  .episodeOne {
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 1;
    font-size: 18px;
    font-weight: 500;
    overflow: hidden;
    line-height: 24px;
  }

  .pb_episode_cont {
    position: relative;
    margin-top: -230px;
  }

  .vjs-icon-cog:before {
    width: 25px !important;
    height: 25px !important;
    top: 7px !important;
  }

  @media only screen and (min-width: 320px) and (max-width: 767px) {
    .play_epsode_btn span img {
      width: 32px !important;
      height: 32px !important;
    }

    .vjs-icon-hd:before {
      width: 22px !important;
      height: 22px !important;
      top: 2px !important;
      display: inline-block;
    }

    .drm-audio-new-wrpaer .vjs-quality-button.vjs-menu-button.vjs-menu-button-popup.vjs-control.vjs-button {
      margin-left: 93%;
      z-index: 9999999999999;
      right: 18px !important;
      top: 109px !important;
    }

    .vjs-icon-cog:before {
      width: 25px !important;
      height: 25px !important;
      top: 5px !important;
      display: inline-block;
    }

    .pb_episode_cont {
      position: relative;
      margin-top: 00px;
    }

    .episodeOne {
      font-size: 15px;
    }

    .share_btn_icon {
      font-size: 16px;
      padding: 0px 12px;
    }

  }

  @media only screen and (min-width: 768px) and (max-width: 1024px) {
    .pb_episode_cont {
      position: relative;
      margin-top: -90px;
    }
  }

  @media only screen and (min-width: 1801px) and (max-width: 2050px) {
    .play_epsode_btn span img {
      width: 54px !important;
      height: 54px !important;
    }

    .share_btn_icon {
      font-size: 24px;
      background: #2b2b2b;
      padding: 0px 17px;
    }

    .music-card {
      max-width: 650px !important;
      width: 100%;
    }

    .music-card__content {
      width: 650px !important;
      height: auto;
    }

    .music-div {
      position: relative;
      width: 100%;
      height: 366px !important;
    }
  }
 @media only screen and (min-width: 1801px){
      .nav-audio-list {
        padding: 0 !important;
        font-size: 24px !important;
    }
 }
  @media only screen and (min-width: 1801px) and (max-width: 2400px) {
    .play_epsode_btn span img {
      width: 54px !important;
      height: 54px !important;
    }

    .share_btn_icon {
      font-size: 24px;
      background: #2b2b2b;
      padding: 0px 17px;
    }

    .music-card {
      max-width: 650px !important;
      width: 100%;
    }

    .music-card__content {
      width: 650px !important;
      height: auto;
    }

    .music-div {
      position: relative;
      width: 100%;
      height: 366px !important;
    }
  }

  @media only screen and (min-width: 2101px) and (max-width: 2400px) {
    .play_epsode_btn span img {
      width: 54px !important;
      height: 54px !important;
    }

    .share_btn_icon {
      font-size: 24px;
      background: #2b2b2b;
      padding: 0px 17px;
    }
  }

  @media only screen and (min-width: 2401px) {
    .play_epsode_btn span img {
      width: 54px !important;
      height: 54px !important;
    }

    .music-card {
      max-width: 650px !important;
      width: 100%;
    }

    .music-card__content {
      width: 650px !important;
      height: auto;
    }

    .music-div {
      position: relative;
      width: 100%;
      height: 366px !important;
    }

    .share_btn_icon {
      font-size: 24px;
      background: #2b2b2b;
      padding: 0px 17px;
      border-radius: 5px;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }

  .player_img {
    display: flex;
    justify-content: center;
    margin-top: 100px;
  }
</style>
<style>
  .music-card__wrapper {
    display: flex;
    align-items: center;
    height: 100%;
  }

  .music-div {
    position: relative;
    width: 100%;
    height: 300px;
  }

  .music-div .music-image {
    width: 100%;
    height: auto !important;
    border-radius: 5px;
    /* margin: auto !important; */
    max-height: 100%;
  }

  .audio-nav {
    position: relative;
    z-index: 11;
  }

  .nav-audio-list {
    border: none !important;
    background: none !important;

  }

  .section {
    position: relative;
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100vh;
  }

  .section__background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
    opacity: 0.5;
  }

  .section__background:before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    backdrop-filter: blur(30px);
    z-index: 11;
  }

  .section__background-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .music-card {
    margin: 1rem auto;
    position: relative;
    max-width: 450px;
    width: 100%;
    height: auto;
    /* border-radius: 25px; */
    transform-style: preserve-3d;
    transition: all .2s linear;
    z-index: 2;
  }

  .music-card.right-weight {
    transform: rotateY(4deg) rotateX(-5deg);
  }

  .music-card.left-weight {
    transform: rotateY(-4deg) rotateX(-5deg);
  }

  .music-card__wrapper {
    position: relative;
    z-index: 1;
  }

  .music-card__content {

    width: 450px;

    height: auto;
  }

  .music-card:before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;

    z-index: -1;
  }

  .music-image {
    position: relative;
    width: 100%;

    object-fit: cover;
    filter: drop-shadow(-20px 10px 10px rgba(0, 0, 0, 0.25)) !important;
  }

  .music-info {
    padding-inline: 20px;
  }

  .music-name {
    font-size: 1.4em;
    color: rgba(255, 255, 255, .8);
    margin-bottom: 4px;
    line-height: 1;
  }

  .music-artist {
    font-size: 1em;
    color: rgba(255, 255, 255, .5);
  }

  .music-controls {
    display: flex;
    justify-content: space-between;
    padding-top: 25px;
  }

  .music-controls-item {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 50px;
    height: 50px;
    padding: 6px;
    border-radius: 50%;
    cursor: pointer;
    transition: ease-in-out .2s;
  }

  .play-icon-background {
    position: absolute;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background: #4845F6;
    z-index: -1;
    opacity: 0;
    pointer-events: none;
    filter: drop-shadow(0px 0px 10px rgba(0, 0, 0, .25));
    transition: all .2s;
  }

  #seek-prev img {
    width: 30px;
    height: 30px;
  }

  #prev img {
    width: 25px;
    height: 25px;
  }

  #seek-next img {
    width: 30px;
    height: 30px;
  }

  #play img {
    width: auto;
    height: 60px;
  }

  #next img {
    width: 25px;
    height: 25px;
  }

  .music-controls-item#play .play-icon {
    filter: drop-shadow(2px 4px 6px rgba(0, 0, 0, .3));
    transition: all .2s;
  }

  .music-controls-item#play:hover .play-icon-background {
    animation-name: playIconBackgroundAnimate;
    animation-duration: .3s;
    animation-iteration-count: 1;
    opacity: 1;
  }

  .music-controls-item#play:hover .play-icon {
    animation-name: playIconAnimate;
    animation-duration: .3s;
    animation-iteration-count: 1;
  }

  .music-controls-item--icon {
    font-size: 1.2em;
    color: #fff;
  }

  .music-progress {
    position: relative;
    width: calc(100% - 0px);
    margin-top: 16px;
    margin-bottom: 10px !important;
    opacity: 0 !important;

  }


  .music-progress-bar {
    position: relative;
    width: 100%;
    height: 5px;
    border-radius: 5px;
    background-color: #4845F6;
    cursor: pointer;
    opacity: 0 !important;
  }

  .music-progress-bar {
    /* border: solid 2px #82CFD0; */
    background: linear-gradient(to right, rgb(72, 69, 246) 0%, rgb(72, 69, 246) 0%, rgb(255, 255, 255) 0%, white 100%);
    background: -webkit-linear-gradient(to right, rgb(72, 69, 246) 0%, rgb(72, 69, 246) 0%, rgb(255, 255, 255) 0%, white 100%);

    border-radius: 8px;
    height: 6px;
    width: 100%;
    outline: none;
    transition: background 450ms ease-in;
    -webkit-appearance: none;
  }

  .music-progress-bar::-webkit-slider-thumb {
    width: 20px;
    height: 20px;
    border-radius: 50%;
    -webkit-appearance: none;
    cursor: pointer;
    background: #4845F6;
  }

  .music-progress-bar::-moz-range-thumb {
    -webkit-appearance: none;
    appearance: none;
    width: 20px;
    height: 20px;
    background: #4845F6;

    border: none;
    border-radius: 50%;
  }


  .music-progress-bar:after {
    /* content: ''; */
    position: absolute;
    right: -6px;
    top: 50%;
    transform: translateY(-50%);
    width: 12px;
    height: 12px;
    background: #4845F6;
    filter: drop-shadow(0px 0px 4px rgba(46, 45, 45, 1));
    border-radius: 50%;
    box-sizing: border-box;
  }

  .music-progress:before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 100%;
    height: 5px;
    background: rgba(255, 255, 255, .3);
    border-radius: 5px;
    z-index: -1;
  }

  .music-progress__time {
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .music-progress__time-item {
    color: #fff;
    font-size: 12px;
    opacity: .4;
  }

  .pb-audio_pl {
    position: absolute;
    top: 50%;
    left: 50%;
    background: rgba(0, 0, 0, 0.7);
    content: "";
    transform: translate(-50%, -50%);
    width: 100%;
    height: 100%;
    font-size: 16px !important;
    color: var(--white);
  }

  .pb_audio_txt {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100%;
  }

  @keyframes coverAnimate {
    0% {
      transform: scale(1);
    }

    50% {
      transform: scale(0.98);
    }

    100% {
      transform: scale(1);
    }
  }

  @-webkit-keyframes coverAnimate {
    0% {
      transform: scale(1);
    }

    50% {
      transform: scale(0.98);
    }

    100% {
      transform: scale(1);
    }
  }

  @keyframes playIconAnimate {
    0% {
      transform: scale(1);
    }

    20% {
      transform: scale(1);
    }

    85% {
      transform: scale(1.2);
    }

    100% {
      transform: scale(1);
    }
  }

  @keyframes playIconBackgroundAnimate {
    0% {
      opacity: 1;
      transform: scale(0.7);
      filter: drop-shadow(0px 0px 10px rgba(0, 0, 0, .1));
    }

    65% {
      transform: scale(1.1);
      filter: drop-shadow(0px 0px 10px rgba(0, 0, 0, .25));
    }

    85% {
      transform: scale(1);
      filter: drop-shadow(0px 0px 10px rgba(0, 0, 0, .18));
      opacity: 1;
    }

    100% {
      transform: scale(1);
      filter: drop-shadow(0px 0px 10px rgba(0, 0, 0, .18));
      opacity: 1;
    }
  }

  @media screen and (max-width: 480px) {
    .music-image {
      width: 100%;
      max-width: calc(100% - 0px);
      /* margin: 20px; */
      left: unset;
      top: unset;
      height: auto;
      /* border-radius: 12px; */
      filter: drop-shadow(0px 10px 10px rgba(0, 0, 0, 0.20));
    }

    .music-card {
      width: 100%;
      max-width: calc(100% - 20px);
      margin-inline: auto;
    }

    .music-div .music-image {
      border-radius: 5px;
      height: auto !important;

      /* max-height: 100%; */
    }

    .music-card__content {
      padding-bottom: 20px;
      width: 100% !important;
      height: auto;
    }

  }

  @media only screen and (min-width: 320px) and (max-width: 767px) {
    .section {
      height: 100vh;
      overflow: auto;
    }

    .music-image {
      width: 100%;
      max-width: calc(100% - 0px);
      /* margin: 20px; */
      left: unset;
      top: unset;
      height: auto;
      /* border-radius: 12px; */
      filter: drop-shadow(0px 10px 10px rgba(0, 0, 0, 0.20));
    }

    .music-card {
      width: 100%;
      max-width: calc(100% - 20px);
      margin-inline: auto;
    }

    .music-div .music-image {
      border-radius: 5px;
      height: auto !important;
      min-height: 190px;

      /* max-height: 100%; */
    }

    .music-card__content {
      padding-bottom: 20px;
      width: 100% !important;
      height: auto;
    }

    .audio-nav {
      top: 0;
      padding: 0 !important;
    }

    .music-card__wrapper {
      height: 100vh;
    }

    .section__background {
      height: 100vh;
    }
  }

  @media only screen and (min-width: 768px) and (max-width: 991px) {
    .vjs-loading-spinner {
      margin-top: -35% !important;
    }
  }

  @media only screen and (min-width: 320px) and (max-width: 450px) {
    .vjs-loading-spinner {
      margin-top: -121px !important;
    }
  }

  .vjs-icon-cog {
    font-family: VideoJS;
    font-weight: normal;
    font-style: initial;
    display: flex;
    justify-content: end;
    margin-top: -41px;
    margin-right: 16px;
  }

  .disable_icon {
    /* pointer-events: none;
    opacity: 0.4;*/
  }

  .ad_seeting {
    display: none;
    border: 1px solid #474747;
  }

  .ad_setting_pb:hover .ad_seeting {
    display: block;
    position: absolute;
    bottom: 40px;
    background: rgba(55, 55, 55, 0.83);
    min-width: 14em;
    border-radius: 5px;
    right: 0;
    z-index: 9999999 !important;
  }

  .ad_seeting ul {
    list-style: none;
    margin: 0;
    padding-left: 0;
  }

  .ad_seeting ul li {
    list-style: none;
    padding: 10px !important;
    font-size: 14px;
  }

  .audioSection {
    display: none;
    position: absolute !important;
    z-index: 999;
    width: 100%;
  }

  .positionRelative {
    position: relative;
  }


  .qualitymenu {

    right: 0;

    background: rgba(55, 55, 55, 0.83) !important;
    position: absolute;
    width: 11em !important;
    max-height: 25em;
    border-radius: 7px;
    border: 1px solid #474747;
    z-index: 99999999;
    display: none;
  }

  .positionRelative:hover .qualitymenu {
    display: block
  }


  .qualitymenu .vjs-menu-item.selected {
    background: #202020;
    color: var(--pbc) !important;
    font-family: 'AnekLatin-SemiBold';
    font-weight: 700;
  }

  .qualitymenu .vjs-menu-item {
    padding: 10px;
    cursor: pointer;
   color: rgba(192, 192, 192, 1)!important;

  }

  .vjs-control-bar {
    display: block !important;
  }

  .drm-audio-new-wrpaer .vjs-quality-button.vjs-menu-button.vjs-menu-button-popup.vjs-control.vjs-button {
    margin-left: 93%;
    z-index: 9999999999999;
    right: 0;
    top: 109px;
  }

  .music-controls-item .ad_setting_pb {
    opacity: 1 !important;
  }

  .add-hove:hover .ad_seeting {
    display: block !important;
    position: absolute;
    bottom: 40px;
    background: rgba(55, 55, 55, 0.83);
    width: 14em;
    border-radius: 5px;
    z-index: 9999999 !important;
    right: 0;
  }

  .vjs-menu-button-popup .vjs-menu .vjs-menu-content {
    bottom: -2em !important;
    right: -44px !important;
    left: inherit !important;
  }

  .vjs-has-started.vjs-user-inactive.vjs-playing .vjs-control-bar {
    visibility: visible;
    opacity: 1 !important;
    transition: visibility 1s, opacity 1s;
  }

  .qualitymenu .vjs-menu-item..selected :first-child {
    border-radius: 7px;
  }

  .qualitymenu .vjs-menu-item.selected :last-child {
    border-radius: 7px;
  }

  .vjs-menu li.vjs-menu-item {
    color: rgba(192, 192, 192, 1) !important;

  }

  .vjs-menu li.vjs-selected {
    color: var(--pbc) !important;
    font-weight: 900;
  }

  .vjs-menu-button-popup .vjs-menu .vjs-menu-content {
    background: rgba(55, 55, 55, 0.98) !important;
  }
</style>

</head>

<body>
  <?php if (isset($video_details['data']) && !empty($video_details['data'])) { ?>
    <?php
    $current_vid = $video_details['data']['id'] ?? null;
    $next_vid = array(
      'id' => 0,
      'similar' => 0,
      'title' => '',
      'poster_url' => '',
      'description' => '',
    );
    $prev_vid = array(
      'id' => 0,
      'similar' => 0,
      'title' => '',
      'poster_url' => '',
      'description' => '',
    );
    ?>
    <?php
    if (isset($content_details['data']['season'])) {
      foreach ($content_details['data']['season'] as $key => $value) {
        foreach ($value['videos'] as $mkey => $mvalue) {
          if ($mvalue['id'] == $current_vid) {
            $next_vid = array(
              'id' => $content_details['data']['season'][$key]['videos'][$mkey + 1]['id'] ?? 0,
              'similar' => 0,
              'title' => $content_details['data']['season'][$key]['videos'][$mkey + 1]['title'] ?? '',
              'poster_url' => $content_details['data']['season'][$key]['videos'][$mkey + 1]['poster_url'] ?? '',
              'description' => $content_details['data']['season'][$key]['videos'][$mkey + 1]['description'] ?? ''
            );
            $prev_vid = array(
              'id' => $content_details['data']['season'][$key]['videos'][$mkey - 1]['id'] ?? 0
            );
            if (($next_vid['id'] == 0) && isset($content_details['data']['season'][$key + 1]['videos'][0]['id'])) {
              $next_vid = array(
                'id' => $content_details['data']['season'][$key + 1]['videos'][0]['id'] ?? 0,
                'similar' => 0,
                'title' => $content_details['data']['season'][$key + 1]['videos'][0]['title'] ?? '',
                'poster_url' => $content_details['data']['season'][$key + 1]['videos'][0]['poster_url'] ?? '',
                'description' => $content_details['data']['season'][$key + 1]['videos'][0]['description'] ?? ''
              );
            }
          }
        }
      }

      if (($next_vid['id'] == 0) && isset($content_details['data']['related'])) {
        $next_vid = array(
          'id' => $content_details['data']['related'][0]['id'] ?? 0,
          'similar' => 1,
          'title' => $content_details['data']['related'][0]['title'] ?? '',
          'poster_url' => $content_details['data']['related'][0]['poster_url'] ?? '',
          'description' => $content_details['data']['related'][0]['description'] ?? ''
        );
      }
    }
    $video_id = $video_details['data']['id'] ?? 0;

    ?>


    <section class="pb-audio-player">

      <div class="conatiner-fluid">
        <div class="row">
          <div class="col-lg-11 mx-auto col-12">
          </div>
        </div>
      </div>

      <div class="section" id="my-audio_pb">

        <div class=" section__background">

          <img id="backgroundImage" class="section__background-image" src="<?= $video_details['data']['poster_url'] ?>" alt="banner image">
        </div>

        <nav class="audio-nav py-0 back-buttontt">
          <!-- <a onclick="update_data()" class="d-flex nav-audio-list text-decoration-none list-group-item d-block text-white"> -->
          <a onclick="history.go(-1)" class="d-flex nav-audio-list back-button text-decoration-none list-group-item d-block  text-white">
            <i class="fa fa-chevron-left text-white"></i>
            <span class="ms-4 text-white"><b><?= $video_details['data']['title'] ?></b></span>
          </a>
        </nav>
        <div class="music-card__wrapper">
          <div class="music-card">
            <div class="music-card__content">
              <div class="music-div">
                <img class="music-image" src="<?= $video_details['data']['poster_url'] ?>" alt="poster url">
                <p class=" audio_bott_dt mb-0">&nbsp;</p>
                <div class="audioLoder ">
                  <div class="loading"></div>
                </div>
              </div>
              <div class="music-info">
                <h2 class="music-name"></h2>
                <p class="music-artist"></p>
              </div>
              <div class="d-non e drm-audio-new-wrpaer" id="my-audio-div">
                <video  data-matomo-title="<?=$content_details['data']['title']."-".$video_details['data']['title'] ?>"
                id="my_video" class="video-js" poster="<?= $video_details['data']['poster_url']; ?>" controls>
                </video>
              </div>
              <div class="music-progress">
                <input type="range" min="0" max="100" step=".5" id="progress-bar" class="music-progress-bar" value="0" style="opacity:0;" />

              </div>
              <div class="music-progress__time">
                <span class="music-progress__time-item music-current-time">00:00</span>
                <span class="music-progress__time-item music-duration-time">00:00</span>
              </div>

              <div class="music-controls">
                <div id="prev" class="music-controls-item disable_icon">
                  <img src="<?= base_url('assets/website_assets/css/video_player_icons/vlolume_h.svg') ?>" alt="language" class="">
                </div>
                <div id="seek-prev" data-id='<?= $prev_vid['id'] ?>' class="music-controls-item <?= ($prev_vid['id'] == 0) ? 'disable_icon' : '' ?>">
                  <a href="<?= base_url('play-media?id=') . aes_cbc_encryption_($prev_vid['id']) . '&type=' . aes_cbc_encryption_('prev') ?>">
                    <img src="<?= base_url('assets/images/previous_icon.svg') ?>" alt="Previous">
                  </a>
                  <!-- <img src="<? //= base_url('assets/images/backword_icon.svg') 
                                  ?>" alt="prev svg"> -->
                </div>

                <div id="play" class="music-controls-item">
                  <img src="<?= base_url('assets/images/play_icon.svg') ?>" alt="play svg">
                  <div class="play-icon-background"></div>
                </div>
                <div id="seek-next" data-id='<?= $next_vid['id'] ?>' class="music-controls-item <?= ($next_vid['id'] == 0) ? 'disable_icon' : '' ?>">
                  <a href="<?= base_url((($next_vid['similar'] != 1) ? 'play-media?id=' : 'play-episode?id=')) . aes_cbc_encryption_($next_vid['id']) . '&type=' . aes_cbc_encryption_('next') ?>">
                    <img src="<?= base_url('assets/images/next_icon.svg') ?>" alt="forward svg">
                  </a>
                </div>

                <div id="next" class="music-controls-item ad_setting_pb <?= ($video_details['data']['is_drm_protected'] == 2) ? 'disable_icon' : ''; ?>">
                  <img src="<?= base_url('assets/images/seeting-ad.svg') ?>" alt="setting svg" class="d-none">
                  <div class="ad_seeting">
                    <!-- <ul>
                      <li class="textQuality disable_icon" disab>Quality</li>
                      <?php foreach ($bandwidth as $key => $value) { ?>
                        <li data-url="<?= $value ?>" class="ch_bit"><?= floor($key / 1000) . ' kbps' ?></li>
                      <?php } ?>
                    </ul> -->
                  </div>
                </div>
              </div>
            </div>
          </div>

        </div>


        <!-- -Section Audio Player -->

        <div class="audioSection audio_pip_icons" id="audio_pip">
          <div class="audioImg">
            <img class="music-image_pip" src="<?= $video_details['data']['poster_url'] ?>" alt="poster image">
            <p class="mb-0 audiopip_hov"></p>
            <div class="audioClose">
              <span class="hide_pip" onclick="$('.audioSection').hide()" onkeypress="handleKeyPress(event)" onkeydown="handleKeyDown(event)" onkeyup="handleKeyUp(event)" tabindex="0" style="cursor: pointer;">
                &times;
              </span>
            </div>

            <div class="backToaudio hide_pip" onclick="hideAudioSection()" onkeypress="handleKeyPress(event)" onkeydown="handleKeyDown(event)" onkeyup="handleKeyUp(event)" tabindex="0" style="cursor: pointer;">
              <p class="m-0">Back to tab</p>
              <img class="img-fluid " src="<?= base_url('assets/images/backToTab.svg') ?>" alt="back to tab" style="cursor: pointer;">
            </div>
            <div class="subAudio" onclick="toggleAudio()" onkeypress="handleKeyPress(event)" onkeydown="handleKeyDown(event)" onkeyup="handleKeyUp(event)" tabindex="0" style="cursor: pointer;">
              <img class="img-fluid" src="<?= base_url('assets/images/play_icon.svg') ?>" style="cursor: pointer;" alt="play image">
            </div>
          </div>

        </div>

    </section>
    <?php
    $guest_user = ($this->session->id) ? 'Audio' : 'GuestUserAudio';
    ?>

    <script src="<?php echo base_url('assets/website_assets/js/video.js'); ?>"></script>
    <script src="<?php echo base_url('assets/website_assets/js/shaka-player.compiled.debug.js'); ?>"></script>
    <script src="<?php echo base_url('assets/website_assets/js/videojs-audio-shaka.js'); ?>"></script>
    <script src="<?php echo base_url('assets/website_assets/js/shaka-player.ui.js'); ?>"></script>
    <script src="<?php echo base_url('assets/website_assets/js/videojs.hotkeys.min.js'); ?>"></script>
    <script src="<?php echo base_url('assets/website_assets/js/videojs-contrib-eme.min.js'); ?>"></script>

    <script>
      var times = '';
      var adParams = <?= $adParams??'' ?>;
      var adEnabled = "<?= $adEnabled??false ?>";
      if(adEnabled.length > 0){
          adEnabled = true;
      }else{
          adEnabled = false;
      }
      if(adEnabled){
          try{
            adParams = JSON.parse(adParams);
          }catch(err){
            adParams = adParams;
          }
          
      }
      //Make the DIV element draggagle:
      <?php
      $time = ($video_details['data']['free_episode'] == 1) ? 86400 : $video_details['data']['free_time'] ?>
      let session = "<?= $this->session->id ?>";
      let is_free = "<?= $video_details['data']['is_free'] ?>";
      let free_time = "<?= $time ?>";
      dragElement(document.getElementById("audio_pip"));

      function base64DecodeUint8Array(input) {
        var raw = window.atob(input);
        var rawLength = raw.length;
        var array = new Uint8Array(new ArrayBuffer(rawLength));

        for (i = 0; i < rawLength; i++)
          array[i] = raw.charCodeAt(i);

        return array;
      }



      function base64EncodeUint8Array(input) {
        var keyStr = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=";
        var output = "";
        var chr1, chr2, chr3, enc1, enc2, enc3, enc4;
        var i = 0;

        while (i < input.length) {
          chr1 = input[i++];
          chr2 = i < input.length ? input[i++] : Number.NaN;
          chr3 = i < input.length ? input[i++] : Number.NaN;

          enc1 = chr1 >> 2;
          enc2 = ((chr1 & 3) << 4) | (chr2 >> 4);
          enc3 = ((chr2 & 15) << 2) | (chr3 >> 6);
          enc4 = chr3 & 63;

          if (isNaN(chr2)) {
            enc3 = enc4 = 64;
          } else if (isNaN(chr3)) {
            enc4 = 64;
          }
          output += keyStr.charAt(enc1) + keyStr.charAt(enc2) +
            keyStr.charAt(enc3) + keyStr.charAt(enc4);
        }
        return output;
      }


      function arrayToString(array) {
        var uint16array = new Uint16Array(array.buffer);
        return String.fromCharCode.apply(null, uint16array);
      }

      function arrayBufferToString(buffer) {
        var arr = new Uint8Array(buffer);
        var str = String.fromCharCode.apply(String, arr);
        return str;
      }

      function dragElement(elmnt) {
        var pos1 = 0,
          pos2 = 0,
          pos3 = 0,
          pos4 = 0;
        if (document.getElementById(elmnt.id)) {
          /* if present, the header is where you move the DIV from:*/
          document.getElementById(elmnt.id).onmousedown = dragMouseDown;
        } else {
          /* otherwise, move the DIV from anywhere inside the DIV:*/
          elmnt.onmousedown = dragMouseDown;
        }

        function dragMouseDown(e) {
          e = e || window.event;
          e.preventDefault();
          // get the mouse cursor position at startup:
          pos3 = e.clientX;
          pos4 = e.clientY;
          document.onmouseup = closeDragElement;
          // call a function whenever the cursor moves:
          document.onmousemove = elementDrag;
        }

        function elementDrag(e) {
          e = e || window.event;
          e.preventDefault();
          // calculate the new cursor position:
          pos1 = pos3 - e.clientX;
          pos2 = pos4 - e.clientY;
          pos3 = e.clientX;
          pos4 = e.clientY;
          // set the element's new position:
          elmnt.style.top = elmnt.offsetTop - pos2 + "px";
          elmnt.style.left = elmnt.offsetLeft - pos1 + "px";
        }

        function closeDragElement() {
          /* stop moving when mouse button is released:*/
          document.onmouseup = null;
          document.onmousemove = null;
        }
      }
    </script>
    <script>
       $('.back-buttontt').click(function(e) {
      e.preventDefault();
      // alert("erferferf");
     // matomo_like('LiveRadio', 'Stop', '<?= $content_details['data']['id'] . "/" . $content_details['data']['title'] ?>', '<?= ($content_details['data']['genres']) ?? "" ?>');
     // window.location.href = "<?= base_url("pb_live?type=radio"); ?>";
    });
      </script>

    <script>
      let addclass = true;
      $(document).ready(function() {
        $('.ad_seeting ul li').click(function() {
          $('.audioLoder').css('display', 'flex');
          $('.ad_seeting ul li').removeClass('selected_ad');
          $(this).addClass('selected_ad');
          addclass = false;
        });
        player.muted(true);
        changeIcon();
        playMusic();
        $('#prev').on('click', function() {
          if (player.muted()) {
            player.muted(false);
          } else {
            player.muted(true);
          }
          changeIcon();
        });
      });

      function changeIcon() {
        var Vicon = "<?php echo base_url('assets/website_assets/css/video_player_icons/vlolume_h.svg'); ?>";
        var Micon = "<?php echo base_url('assets/website_assets/css/video_player_icons/mute1.svg'); ?>";
        if (player.muted()) {
          $('#prev img').attr('src', Micon);
        } else {
          $('#prev img').attr('src', Vicon);
        }
      }
      const next = document.querySelector("#next");
      const play = document.querySelector("#play");
      const prev = document.querySelector("#prev");
      const progressBar = document.querySelector("#progress-bar");
      const musicCard = document.querySelector(".music-card");
      const musicCover = document.querySelector(".music-image");
      const musicCurrentTime = document.querySelector(".music-current-time");
      const musicDurationTime = document.querySelector(".music-duration-time");
      const backgroundImage = document.querySelector("#backgroundImage");
      const music = document.querySelector("#my_video");
      const progressZone = document.querySelector(".music-progress");
      const imgElement = play.querySelector("img");
      const pipimgElement = document.querySelector(".subAudio").querySelector("img");
      var player;
      let isPlaying = false;
      // default select first music
      let selectedMusic = 1;
      let url = "<?= $video_details['data']['file_url'] ?>";
      let type = "<?= $video_details['data']['is_drm_protected'] ?>";
      let token = "<?= $video_details['data']['token'] ?? null; ?>";
      var base_url = "<?= $baseurl ?>";


      const loadMusic = (url, type, token = null) => {
        if (type == 1) {
          licenseUri = `<?= BASEURLAPI ?><?= BASEVERSION ?>onRequestCreateVideoLicense`;
          widevineToken = token;
          var fairplayCertUri = 'https://license-global.pallycon.com/ri/fpsKeyManager.do?siteId=2LSX';
          dashUri = url;
          if (dashUri.includes('.m3u8')) {
            player = videojs('my_video');
            player.eme();
            playerConfig = {
              // controls: true,
              src: dashUri,
              playbackRates: [0.5, 1, 1.5, 2, 4],
              type: 'application/x-mpegurl',
              keySystems: {
                'com.apple.fps.1_0': {
                  getCertificate: function(emeOptions, callback) {
                    videojs.xhr({
                      url: fairplayCertUri,
                      method: 'GET',
                    }, function(err, response, responseBody) {
                      if (err) {
                        callback(err)
                        return
                      }
                      callback(null, base64DecodeUint8Array(responseBody));
                    })
                  },
                  getContentId: function(emeOptions, initData) {
                    const contentId = arrayToString(initData);
                    return contentId.substring(contentId.indexOf('skd://') + 6);
                  },
                  getLicense: function(emeOptions, contentId, keyMessage, callback) {
                    videojs.xhr({
                      url: licenseUri,
                      method: 'GET',
                      method: 'POST',
                      responseType: 'text',
                      body: 'spc=' + base64EncodeUint8Array(keyMessage),
                      headers: {
                        'Content-type': 'application/x-www-form-urlencoded',
                        'pallycon-customdata-v2': widevineToken
                      }
                    }, function(err, response, responseBody) {
                      if (err) {
                        callback(err)
                        return
                      }
                      callback(null, base64DecodeUint8Array(responseBody))
                    })
                  }
                }
              }
            };
            player.qualityPickerPlugin();

            player.src(playerConfig);
          } else {
            player = videojs('my_video', {
              techOrder: ['shaka'],
              headers: {
                'custom-header': 'some value'
              },
              // playbackRates: [0.5, 1, 1.5, 2, 4],
              shaka: {
                debug: false,
                sideload: true,
                configuration: {
                  drm: {
                    servers: {
                      'com.widevine.alpha': licenseUri
                    },
                    advanced: {
                      'com.widevine.alpha': {
                        'videoRobustness': 'SW_SECURE_CRYPTO',
                        'audioRobustness': 'SW_SECURE_CRYPTO'
                      }
                    }
                  },
                },
                licenseServerAuth: function(type, request) {
                  request.headers['pallycon-customdata-v2'] = widevineToken;
                  if (type === shaka.net.NetworkingEngine.RequestType.LICENSE) {}
                }
              }
            });
            player.src([{
              type: 'application/dash+xml',
              src: dashUri
            }]);
            player.qualityPickerPlugin();
          }
        } else {
          player = videojs('my_video', {
            autoplay: false,
            sources: [{
              src: url,
              type: 'application/x-mpegURL'
            }]
          });

          player.ready(function() {
            var currentQualityIndex = -1;

            this.hotkeys({
              volumeStep: 0.1,
              seekStep: 5
            });
          });


          player.one("loadedmetadata", () => {
            calidades = player.hls.representations();
            //console.log(calidades)
            crearBotonesCalidades({
              class: "vjs-menu-item",
              calidades: calidades,
              // father: player.controlBar.el_,
            }, calidades);
          });

          function crearBotonAutoCalidad(params, calidades) {
            let button = document.createElement("div");
            button.id = "auto";
            button.innerText = `Auto`;
            button.classList.add("selected");
            if (params && params.class) button.classList.add(params.class);
            button.addEventListener("click", () => {
              removeSelected(params);
              button.classList.add("selected");
              calidades.map((calidad) => calidad.enabled(true));
            });
            return button;
          }

          function crearBotonesCalidades(params, calidades) {
            let contentMenu = document.createElement("div");
            let menu = document.createElement("div");
            let icon = document.createElement("div");
            let fullscreen = document.querySelector(".music-controls");
            fullscreen.after('');
            contentMenu.appendChild(icon);
            contentMenu.appendChild(menu);
            fullscreen.after(contentMenu);
            menu.classList.add("qualitymenu");
            icon.classList.add("icon", "vjs-icon-cog");
            contentMenu.classList.add("positionRelative");
            let botonAuto = crearBotonAutoCalidad(params, calidades);
            menu.appendChild(botonAuto);
            calidades.sort((a, b) => {
              return a.bandwidth > b.bandwidth ? 1 : 0;
            });
            calidades.map((calidad) => {
              let button = document.createElement("div");
              if (params && params.class) button.classList.add(params.class);
              button.id = `${calidad.bandwidth}`;
              button.innerText = Math.floor(calidad.bandwidth / 1000) + "Kbps";
              button.addEventListener("click", () => {
                resetCalidad(params);
                button.classList.add("selected");
                calidad.enabled(true);
              });
              menu.appendChild(button);
            });
            // setInterval(() => {
            let auto = document.querySelector("#auto");
            document.querySelector("#auto").innerHTML = "Auto";

          }

          function removeSelected(params) {
            document.querySelector("#auto").classList.remove("selected");
            [...document.querySelectorAll(`.${params.class}`)].map((calidad) => {
              calidad.classList.remove("selected");
            });
          }

          function resetCalidad(params) {
            removeSelected(params);

            for (let calidad of params.calidades) {
              calidad.enabled(false);
            }
          }

        }
      }


      loadMusic(url, type, token);

      play.addEventListener("click", () => {
        isPlaying ? pauseMusic() : playMusic();
      });

      $('.hide_pip').on('click', function() {
        $('.music-image').css('filter', 'blur(0px)');
      });

      $('.music-div').on('click', function() {
        isPlaying ? pauseMusic() : playMusic();
      });

      let spacePressed = false;
      $(document).on("keyup", function() {
        spacePressed = false;
      });

      $(document).on("keydown", async (e) => {
        const playerVolume = player.volume();
        const playerCurrentTime = player.currentTime();
        switch (e.code) {
          case "Space":
            e.preventDefault();
            if (!spacePressed) {
              spacePressed = true;
              isPlaying ? pauseMusic() : playMusic();
            }
            break;
          case "ArrowUp":
            e.preventDefault();
            player.muted(false);
            player.volume(playerVolume + 0.1);
            // logVolume()

            break;
          case "ArrowDown":
            e.preventDefault();
            player.volume(playerVolume - 0.1);
            // logVolume()
            break;
          case "KeyM":
            e.preventDefault();
            if (player.muted()) {
              player.muted(false);
            } else {
              player.muted(true);
            }
            // logVolume()
            break;
          case "ArrowRight":
            e.preventDefault();
            await player.currentTime(playerCurrentTime + 10);
            await animateNotificationIn(false);
            break;
          case "ArrowLeft":
            e.preventDefault();
            await player.currentTime(playerCurrentTime - 10);
            await animateNotificationIn(true);
            break;

          default:
            return;
        }
      });
      var isDragging = false;
      var time_update = true;
      $('.music-progress').on('mousedown', function(event) {
        isDragging = true;
      });

      $('.music-progress').on('mousemove', function(event) {
        if (isDragging) {
          var seekPosition = (event.offsetX / this.offsetWidth) * player.duration();
          var progressPercent = (seekPosition / player.duration()) * 100;
          progressPercent = Math.round(progressPercent);
          progressBar.addEventListener('input', function() {
            const value = this.value;
            this.style.background = `linear-gradient(to right, #4845F6 0%, #4845F6 ${progressPercent}%, #fff ${progressPercent}%, white 100%)`
          })
        }
      });

      $('.music-progress').on('mouseup', function(event) {
        var seekPosition = (event.offsetX / this.offsetWidth) * player.duration();
        player.currentTime(seekPosition);
        isDragging = false;
      });
      var lastPlay = 0;
      const playMusic = () => {
        player.play();
        times = "/" + convertTimes(player.currentTime());
        if (lastPlay > 1) {
          var  similar_to = getQueryParam('similar') || 'NA';
        if(similar_to !='NA'){
          var search_jao = similar_to;
          //console.log(search_jao);
        }
        else{
          var search_jao = '';
        }
      //console.log(similar_to);
          if ("<?= $video_details['data']['title'] ?>") {
            if("<?= $video_details['data']['is_paid']==0 ?>"){
              
              queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Resume', "<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(AVOD)'.'/Podcast' ?>" + times]);

            }
          else if("<?= $video_details['data']['is_paid']==1 ?>"){
            queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Resume', "<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(SVOD)'.'/Podcast' ?>" + times]);
          }
          else{
            queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Resume', "<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(TVOD)'.'/Podcast' ?>" + times]);
          }
        }
        } else {
          var  similar_to = getQueryParam('similar') || 'NA';
        if(similar_to !='NA'){
          var search_jao = similar_to;
          //console.log(search_jao);
        }
        else{
          var search_jao = '';
        }
      //console.log(similar_to);
          if ("<?= $video_details['data']['title'] ?>") {
            if("<?= $video_details['data']['is_paid']==0 ?>"){
              
              queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Play', "<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(AVOD)'.'/Podcast' ?>" + times]);
            }
          else if("<?= $video_details['data']['is_paid']==1 ?>"){
            queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Play', "<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(SVOD)'.'/Podcast' ?>" + times]);
          }
          else{
            queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Play', "<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(TVOD)'.'/Podcast' ?>" + times]);
          }
        }
        }
        imgElement.src = "<?= base_url('assets/images/pause_icon.svg') ?>";
        pipimgElement.src = "<?= base_url('assets/images/pause_icon.svg') ?>";
        isPlaying = true;
        fadeInCover();
        musicCard.classList.add("middle-weight");
        setTimeout(() => {
          musicCard.classList.remove("middle-weight");
        }, 200);
      };

      const pauseMusic = () => {
        times = "/" + convertTimes(player.currentTime());

        if ("<?= $video_details['data']['title'] ?>") {
          var  similar_to = getQueryParam('similar') || 'NA';
        if(similar_to !='NA'){
          var search_jao = similar_to;
          //console.log(search_jao);
        }
        else{
          var search_jao = '';
        }
      //console.log(similar_to);
            if("<?= $video_details['data']['is_paid']==0 ?>"){
              
              queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Pause', "<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(AVOD)'.'/Podcast' ?>" + times]);

            }
          else if("<?= $video_details['data']['is_paid']==1 ?>"){
            queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Pause', "<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(SVOD)'.'/Podcast' ?>" + times]);
          }
          else{
            queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Pause', "<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(TVOD)'.'/Podcast' ?>" + times]);
          }
        }
        player.pause();
        imgElement.src = "<?= base_url('assets/images/play_icon.svg') ?>";
        pipimgElement.src = "<?= base_url('assets/images/play_icon.svg') ?>";
        isPlaying = false;
        fadeInCover();
        musicCard.classList.add("middle-weight");
        setTimeout(() => {
          musicCard.classList.remove("middle-weight");
        }, 200);
      };


      const nextMusic = () => {

      };

      const prevMusic = () => {
        $('.music-image').css('filter', 'blur(3px)');
        $('.audioSection').show('slow');

      };



      const fadeInCover = () => {
        musicCover.classList.add("animate");
        setTimeout(() => {
          musicCover.classList.remove("animate");
        }, 300);
      };

      const updateProgress = (e) => {
        const duration = player.duration();
        const currentTime = player.currentTime();
        var progressPercent = (currentTime / duration) * 100;
        progressPercent = Math.round(progressPercent);
        progressBar.value = progressPercent;
        progressBar.style.background = `linear-gradient(to right, #4845F6 0%, #4845F6 ${progressPercent}%, #fff ${progressPercent}%, white 100%)`

      };

      var uuid = "<?= $_SESSION['uuid'] ?? 0 ?>";
      var login_chcek = uuid;
      uuid = uuid + "<?= $video_id ?>";

      lastPlay = localStorage.getItem('lastPlayTime' + uuid);
      player.currentTime(lastPlay);
      player.on('timeupdate', function(event) {

        localStorage.setItem('lastPlayTime' + uuid, player.currentTime());
        var Ctime = Math.ceil(player.currentTime());
        is_free = 1;
        if (Ctime > free_time && !session && (Ctime > 1)) {
          player.pause();
          Swal.fire({
            text: "<?= $this->lang->line('free_create_account') ?>",
            title: "<?= $this->lang->line('free_time_up') ?>",
            imageUrl: "<?= base_url('assets/images/timeer.svg') ?>",
            imageWidth: 70,
            imageHeight: 70,
            imageAlt: "Custom image",
            showCancelButton: true,
            showConfirmButton: true,
            confirmButtonText: "<?= $this->lang->line('Login') ?>",
            cancelButtonText: "<?= $this->lang->line('Cancel') ?>"
          }).then(async (result) => {
            var redirect_url = '';
            if (result.value) {
              // matomo('Page', 'View', 'LoginPopup', 5);
              queueTrackingData('trackEvent', ["Page", 'View', "LoginPopup"]);

              redirect_url = "<?= $_SERVER['REQUEST_SCHEME'] . '://' . $_SERVER['HTTP_HOST'] . $_SERVER['REQUEST_URI'] ?>" + '&dur=' + Ctime;
              await set_userdata(redirect_url);
              window.location.href = "<?= base_url('user-login') ?>";
            } else if (result.dismiss) {
              // matomo('Page', 'View', 'CancelPopup', 5);
              queueTrackingData('trackEvent', ["Page", 'View', "CancelPopup"]);

              await set_userdata(redirect_url);
              player.currentTime(0);
              pauseMusic();
            }
          });
        }

        if (time_update) {
          updateProgress(event);
          setMusicTime(event);
        } else {
          time_update = true;
        }
      });

      player.on('ended', function(event) {
        pauseMusic();
        progressBar.value = 0
        progressBar.style.width = `0%`;

        calcSongTime(0, musicCurrentTime);
      });




      progressBar.addEventListener('input', function(e) {

        const value = this.value;
        const setPoint = e.offsetX;
        this.style.background = `linear-gradient(to right, #4845F6 0%, #4845F6 ${value}%, #fff ${value}%, white 100%)`;
        const duration = player.duration();
        player.currentTime((setPoint / value) * duration);
      })

      // Set time area
      const setMusicTime = (e) => {
        // var { duration, currentTime } = e.srcElement;
        const duration = player.duration();
        lastPlay = localStorage.getItem('lastPlayTime' + uuid);
        if ("<?= $this->session->id ?>") {
          if ("<?= $dur ?>") {
            lastPlay = "<?= $dur ?>";
          }
        }
        const currentTime = lastPlay; //player.currentTime();
        if (isNaN(duration)) {
          duration = 0;
        }
        calcSongTime(duration, musicDurationTime);
        calcSongTime(currentTime, musicCurrentTime);
      };

      const calcSongTime = (time, selectTime) => {
        time = Number(time);
        const m = Math.floor((time % 3600) / 60);
        const s = Math.floor((time % 3600) % 60);
        if (m < 10) {
          minute = "0" + m;
        } else minute = m;
        if (s < 10) {
          second = "0" + s;
        } else second = s;

        return (selectTime.textContent = `${minute}:${second}`);
      };

      next.addEventListener("click", nextMusic);
      prev.addEventListener("click", prevMusic);

      function cardAnimate(e) {
        this.querySelectorAll(".music-card").forEach(function(boxMove) {
          const x = -(window.innerWidth / 3 - e.pageX) / 90;
          const y = (window.innerHeight / 3 - e.pageY) / 30;
          boxMove.style.transform = "rotateY(" + x + "deg) rotateX(" + y + "deg)";
        });
      }
    </script>


    <div class="container-fluid">
      <div class="row">
        <div class="col-md-12">
          <?php
          $id = aes_cbc_encryption_($video_details['data']['id']);
          $watch_id = aes_cbc_encryption_($video_details['data']['show_id']);
          ?>
          <input type="hidden" id="product_id" value="<?= $id ?>">
          <input type="hidden" id="watch_id" value="<?= $watch_id; ?>">
        </div>
      </div>
    </div>


  <?php } else { ?>
    <div class="col-md-6 m-auto text-center watchListNo">
      <div class="no_dt_found">
        <img src="<?= base_url('assets/images/no_list_found.png'); ?>" class="img-fluid" alt="no data found">
        <h5 class="m-0 text-center text-white"><?= NoDataFound; ?></h5>
        <p class="mb-0 text_ac"><?= NoListFound; ?></p>
      </div>
    </div>
  <?php } ?>



  <script>
    $('.play-music').on('click', function() {
      var id = $(this).data('id');
      if ($.isNumeric(id)) {
        $.ajax({
          url: "<?= base_url('web/dashboard/getMediaUrl') ?>",
          type: "post",
          data: {
            "id": id
          },
          success: function(res) {
            data = JSON.parse(res);


          }
        }).done(function() {
          setTimeout(function() {
            $("#overlayonajaxhit").fadeOut(300);
          }, 500);
        });
      }
    })
  </script>
  <script>
    $(document).ready(function() {
      window.addEventListener('online', handleConnectionChange);
      window.addEventListener('offline', handleConnectionChange);


      $('.back-button').click(function() {
        if ("<?= $video_details['data']['title'] ?>") {
            if("<?= $video_details['data']['is_paid']==0 ?>"){
                   queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Stop',"<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(VOD)'.'/Podcast' ?>" + times]);
            }
          else if("<?= $video_details['data']['is_paid']==1 ?>"){
            queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Stop',"<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(SVOD)'.'/Podcast' ?>" + times]);
          }
          else{
            queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Stop',"<?= $video_details['data']['id']?>"+'/'+"<?= $video_details['data']['title'].'(TVOD)'.'/Podcast' ?>" + times]);
          }
        }
      })
    });




    function handleConnectionChange(event) {
      if (event.type == "offline") {
        $('#my-audio_pb').append('<h4 class="no_intrnt_text text-white network_bott"><?= $this->lang->line("nointernet-connection") ?></h4>');
        //console.log('offline ==>');
        //overlay("Please wait.. internet is Not available.");
      }
      if (event.type == "online") {
        $('#my-audio_pb').find('.no_intrnt_text').remove();
        $('#my-audio_pb').append('<h4 class="intrnt_text text-white network_bott"><span class="network_size">✓</span> <?= $this->lang->line("internet-connection") ?></h4>');
        setTimeout(() => {
          $('#my-audio_pb').find('.intrnt_text').remove();
        }, 1000)
        //overlay("");
        //console.log('online ==>');
      }
    }


    function matomo(user, type, title, hits = 4) {
      $.ajax({
        type: 'POST',
        url: "<?= base_url('/web/Home/matomo_hit') ?>",
        dataType: "json",
        data: {
          user: user,
          types: type, // Typo here, it should be type instead of types
          type: hits,
          title: title
        },
        success: function(data) {
          if (data.status == 1) {}
        }
      });
    }

    function set_userdata(url) {
      $.ajax({
        url: "<?= base_url('web/login_register/set_session') ?>",
        type: "post",
        data: {
          url
        },
        success: function(res) {

        }
      })
    }

    function convertTimes(timeInSeconds) {
      var minutes = Math.floor(timeInSeconds / 60);
      var seconds = Math.round(timeInSeconds % 60);
      var hours = Math.floor(minutes / 60);
      minutes = minutes % 60;

      // Add leading zeros to hours, minutes, and seconds
      var formattedHours = ('0' + hours).slice(-2);
      var formattedMinutes = ('0' + minutes).slice(-2);
      var formattedSeconds = ('0' + seconds).slice(-2);

      // Combine the formatted components into the desired format
      var time = formattedHours + ':' + formattedMinutes + ':' + formattedSeconds;
      return time;
    }

    function hideAudioSection() {
      document.querySelector('.audioSection').style.display = 'none';
    }

    function toggleAudio() {
      if (isPlaying) {
        pauseMusic();
      } else {
        playMusic();
      }
    }
  </script>
  <script>
    $(document).ready(function() {
      $('#seek-next').click(function() {
        var id = $(this).data('id');
        if (id != 0 || id != "") {
          if("<?= $video_details['data']['is_paid']==0 ?>"){
          // matomo("<?= $guest_user ?>", 'Next', id +'/'+"<?= $video_details['data']['title'].'(VOD)'.'/Podcast' ?>" + times, 5);
          queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Next', '<?= $video_details['data']['id'] ?>' + '/'+ '<?= $video_details['data']['title'] ?>' +'-' + '<?= $content_details['data']['title'] ?>' + '(VOD)' +'/' +'Podcast' + '/' +  times]);

          }
          else if("<?= $video_details['data']['is_paid']==1 ?>"){
            queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Next', '<?= $video_details['data']['id'] ?>' + '/'+ '<?= $video_details['data']['title'] ?>' +'-' + '<?= $content_details['data']['title'] ?>' + '(SVOD)' +'/' +'Podcast' + '/' +  times]);
 
          }
          else{
            queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Next', '<?= $video_details['data']['id'] ?>' + '/'+ '<?= $video_details['data']['title'] ?>' +'-' + '<?= $content_details['data']['title'] ?>' + '(TVOD)' +'/' +'Podcast' + '/' +  times]);

          }
        }
         else {
          queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Next', '<?= $video_details['data']['id'] ?>' + '/'+ '<?= $video_details['data']['title'] ?>' +'-' + '<?= $content_details['data']['title'] ?>' + '(VOD)' +'/' +'Podcast' + '/' +  times]);


        
      }
      });
    });
    $(document).ready(function() {
      $('#seek-prev').click(function() {
        var id = $(this).data('id');
        if (id != 0 || id != "") {
          queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Previous', '<?= $video_details['data']['id'] ?>' + '/'+ '<?= $video_details['data']['title'] ?>' +'-' + '<?= $content_details['data']['title'] ?>' + '(VOD)' +'/' +'Podcast' + '/' +  times]);


        } else {
          queueTrackingData('trackEvent', ["<?= $guest_user ?>", 'Previous', '<?= $video_details['data']['id'] ?>' + '/'+ '<?= $video_details['data']['title'] ?>' +'-' + '<?= $content_details['data']['title'] ?>' + '(VOD)' +'/' +'Podcast' + '/' +  times]);


        }
      });
    });

    $(document).ready(function() {

      // Add event listener to the subtitles button
      $(document).on('click', '.vjs-button', function() {
        var menu = $(this).next('.vjs-menu');
        menu.removeClass('vjs-lock-showing');
      });

    });


    $(document).ready(function() {
      // Add event listener to the subtitles button
      if ($(window).width() <= 1024) {
        $(document).on('click', '.vjs-button', function() {

          var menu = $(this).next('.vjs-menu');

          // Check if the menu is currently visible or hidden
          if (menu.hasClass('d-none') || !menu.hasClass('d-block')) {
            menu.removeClass('d-none').addClass('d-block');
          } else {
            menu.removeClass('d-block').addClass('d-none');
          }

        });
      }
    });
  </script>
  <script>
    // Get the element by its class name or ID
    var hidePipSpan = document.querySelector('.hide_pip'); // Example using class selector

    hidePipSpan.addEventListener('keypress', handleKeyPress);
    hidePipSpan.addEventListener('keydown', handleKeyDown);
    hidePipSpan.addEventListener('keyup', handleKeyUp);

    // Define event handler functions
    function handleKeyPress(event) {
      if (event.key === 'Enter') {
        $('.audioSection').hide();
      }
    }

    function handleKeyDown(event) {
      // Your logic for key down event handling
    }

    function handleKeyUp(event) {
      // Your logic for key up event handling
    }
    function getQueryParam(param) {
    const urlParams = new URLSearchParams(window.location.search);
    return urlParams.get(param);
}
function matomo_sear(user, type, titles, geners = '',search_jao) {
    var url = '<?= base_url("/web/Home/matomo_hit"); ?>';
    $.ajax({
      url: url,
      type: "POST",
      dataType: "json",
      async: "true",
      data: {
        user: user,
        types: type, // Typo here, it should be type instead of types
        geners: geners,
        title: titles,
        search_jao :search_jao
        
      },
      success: function(data) {
      },
      error: function(xhr, status, error) {
        //  console.error("Error: " + error);
      }
    });
  }
  </script>
</body>

</html>